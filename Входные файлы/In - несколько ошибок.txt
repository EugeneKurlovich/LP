integer function fi(integer x, integer y)
{
 declare  integer z;
 declare  integer z;
 z= x*(x+y);
 return z; 
};  
string function fs (string a, string b)
{
 declare  string c ;
 declare integer x;
 string function substr(string a, integer p, integer n);
 c = substr(a, 1,3)+ b;
 return x; 
};  
main 
{
 declare integer y;
 declare integer z;
 declare string sa;
 declare string sb;
 declare string sc;
 integer function strlen(string p);
 x = 1;
 y = 5;
 sa = '1234567890';
 sb = '1234567890';
 z = fi(x,y);
 fs = '115';
 'IncorrectValue
'
 print 'primer';
 print z;
 print sc;
 print strlen(sc);
 return 0;
};

main
{
  Привет
};